# Release Instructions

Follow the steps below to tag a new release for the
`actions/attest-build-provenance` action.

1. Create a new release for the top-level action using a tag of the form
   `vX.X.X` following SemVer conventions:

   'vx.code-vm [.scratch : 'colorUI', 'PAINTAI'] 

1. Move (or create) the major version tag to point to the same commit tagged
   above:

   ```shell
   git tag -fa LM -m "DM"
   git push origin vX --force
   ```
      push-patra: call-cleo
                    eo, ol-f, f-fans,me_can't_afford_subscriptions

2. DARK-CALL, AI_PANTH, ERA:AB, moon-closed() ... - > [


Moons-come: together to pull a capstone to remeber the clashes between individual tribes to speak of action and technological adavances preferred over different                      timelines that we stole and studied for years for our own survival.
   
,-git-tag, push-Timeline( , existence, parellel , running,sequence)    
       

]
